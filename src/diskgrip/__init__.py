"""diskgrip — visual block-storage management for Linux.

A sibling to netgrip in spirit and structure: probe the host (read-only),
model it as plain dataclasses, and turn every change into an explicit command
*plan* the user confirms before anything runs. ``core/`` is Qt-free and
headless-testable.
"""

__version__ = "0.0.1"
